class login {
    var email;
    var senha;
    myssql.connection (ipBanco, hostname)

    if(email){
        this.email = email;
        this.senha = senha;

        //senha existe;
        //senha acessa;
    }
}